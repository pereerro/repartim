services:
  sardanes-web:
    image: nginx:alpine
    container_name: sardanes-web
    restart: unless-stopped
    volumes:
      - ./:/usr/share/nginx/html:ro
    networks:
      - nginx-proxy-net

networks:
  nginx-proxy-net:
    external: true