services:
  sardanes-web:
    image: nginx:alpine
    container_name: sardanes-web
    restart: unless-stopped
    volumes:
      - ./:/usr/share/nginx/html:ro
    networks:
      - npm-network

networks:
  npm-network:
    external: true